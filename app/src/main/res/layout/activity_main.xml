<?xml version="1.0" encoding="utf-8"?>
<!--
    CoordinatorLayout es OBLIGATORIO.
    Permite que el BottomAppBar y el FloatingActionButton
    interactúen correctamente entre sí.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:clipChildren="false"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!--
        FragmentContainerView: Aquí es donde se mostrarán tus pantallas.
        Ocupa todo el espacio, y la barra de navegación se superpondrá abajo.
    -->

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?attr/colorSurface"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- El FragmentContainerView va dentro del AppBarLayout -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/nav_host_fragment"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:defaultNavHost="true"
            app:navGraph="@navigation/nav_graph" />

    </com.google.android.material.appbar.AppBarLayout>


    <!--
        BottomAppBar: Este es el componente CORRECTO.
        - Le damos el ID 'bottom_app_bar' para que el FAB pueda encontrarlo.
        - La altura debe ser 'wrap_content'.
        - Los atributos 'fab...' crean el corte para el botón.
    -->
    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bottom_app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:hideOnScroll="true"
        app:backgroundTint="@color/bottomAppBarColor"
        app:fabAlignmentMode="center"
        app:fabCradleMargin="8dp"
        app:fabCradleRoundedCornerRadius="20dp"
        app:fabAnchorMode="cradle">

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            app:labelVisibilityMode="labeled"
            app:menu="@menu/bottom_nav_menu" />

    </com.google.android.material.bottomappbar.BottomAppBar>

    <!--
        FloatingActionButton (FAB): El botón flotante con el ícono de '+'.
        - 'app:layout_anchor' ahora apunta al ID correcto, '@id/bottom_app_bar'.
    -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/md_theme_tertiaryFixed"
        android:contentDescription="@string/floating_add"
        app:tint="@color/md_theme_onTertiaryFixed"
        android:src="@drawable/ic_add"
        app:layout_behavior="com.google.android.material.floatingactionbutton.FloatingActionButton$Behavior"
        app:layout_anchor="@id/bottom_app_bar"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.App.Circle" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>