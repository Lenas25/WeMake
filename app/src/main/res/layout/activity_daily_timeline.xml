<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#7986CB"> <!-- Color Azul Suave (ajusta a tu gusto) -->

    <!-- Toolbar simple -->
    <LinearLayout
        android:id="@+id/header_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/btn_back"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_arrow_back"
            app:tint="@android:color/white"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Timeline"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:gravity="center"
            android:textStyle="bold"/>
    </LinearLayout>

    <!-- Lista Horizontal de Fechas -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_dates"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/header_layout"
        android:paddingBottom="20dp"
        android:clipToPadding="false"
        android:paddingHorizontal="8dp"/>

    <!-- Hoja Blanca de Contenido -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/recycler_dates"
        android:background="@drawable/bg_bottom_sheet_rounded"
        android:orientation="vertical"
        android:paddingTop="24dp"
        android:layout_marginTop="-10dp"> <!-- Efecto de solapamiento -->

        <!-- Encabezado de la lista -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="24dp"
            android:layout_marginBottom="16dp">

            <TextView
                android:id="@+id/tv_today_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Plan del día"
                android:textSize="18sp"
                android:textColor="#424242"/>

            <TextView
                android:id="@+id/tv_task_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:text="0/0"
                android:textSize="16sp"
                android:textColor="#757575"/>

            <!-- Barra rosa decorativa -->
            <View
                android:layout_width="match_parent"
                android:layout_height="2dp"
                android:background="#E91E63"
                android:layout_below="@id/tv_today_label"
                android:layout_marginTop="8dp"/>
        </RelativeLayout>

        <!-- Lista de tareas Vertical -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_daily_tasks"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"/>
    </LinearLayout>


</RelativeLayout>